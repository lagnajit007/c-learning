#include<stdio.h>
void main()
{
    float R; //  defiened the Radius as R
    printf("enter the value: "); //entering the value which i wabt to calculate
    scanf("%f",&R); //storing the value of R in the allocated address (after i put the value)
    printf("the area of circle is: %f", 3.14*R*R); 
 /* calculating the value of the radius through --> 3.14*R*R and
 showing in the screen through %f*/
}
// Area of circle = ⨅r²