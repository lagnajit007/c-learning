#include<stdio.h>
void main()
{
float A; // Here i defiened side as A. 
 
printf("A = "); //entering the value of side

scanf("%f",&A); // storing the value in the machine for the following variable "A"

printf("Area of square is : %f",A*A); //calculating the storing value (A*A) and showing in the screen through "%f"
/* the formula of "Area of square = side*side" */

}