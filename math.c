#include<stdio.h>
void main()
{
    int a , b, c,d; // declaring 4 variables, a,b,c to store value and d to print the ans
    
    printf("a= \n"); //enter the val of a 
    scanf("%d",&a); // input the user given value into a
     printf("b= \n");
    scanf("%d",&b); // input the user given value into b
     printf("c= \n");
    scanf("%d",&c); // input the user given value into c
    printf("ans is: %d", d= a+b*c ); // using the rule after comma and print the value using %d
}
